import { StyleSheet } from 'react-native';

const estilos = StyleSheet.create({
    modal: {
        backgroundColor: "#000",
        margin: 20,
        padding: 20,
        borderRadius: 20,
        elevation: 10
    },
    textoModal: {
        color: "#fff",
        textAlign: 'center'
    }

});

export default estilos;
